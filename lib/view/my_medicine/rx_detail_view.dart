import 'package:flutter/material.dart';

class RXDetailView extends StatelessWidget {
  const RXDetailView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
