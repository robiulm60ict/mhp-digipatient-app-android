import 'package:flutter/material.dart';

class CScaffold extends StatelessWidget {
  const CScaffold({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
